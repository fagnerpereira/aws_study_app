<%= turbo_frame_tag "lesson_progress_bar" do %>
  <div class="flex-1 max-w-md mx-8">
    <div class="bg-gray-200 rounded-full h-3 relative overflow-hidden">
      <div class="progress-bar bg-gradient-to-r from-green-400 to-blue-500 h-3 rounded-full"
           style="width: <%= @completion_percentage %>%"></div>
      <div class="absolute inset-0 flex items-center justify-center">
        <span class="text-xs font-black text-gray-700"><%= @completion_percentage.round %>%</span>
      </div>
    </div>
    <div class="text-xs text-gray-500 text-center mt-1 font-semibold">
      <%= @correct_answers %>/<%= @total_questions %> completed
    </div>
  </div>
<% end %>